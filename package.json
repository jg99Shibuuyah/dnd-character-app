{
  "name": "dnd-character-sheet-server",
  "version": "1.0.0",
  "description": "Local server for the dynamic D&D character sheet, with SQLite-backed character profiles.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "start:noauth": "SKIP_AUTH=true node src/server.js",
    "build": "npm ci --prefix client && npm run build --prefix client",
    "client:dev": "npm run dev --prefix client",
    "test:client": "npm test --prefix client"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "better-sqlite3": "^12.11.1",
    "ejs": "^6.0.1",
    "express": "^4.19.2"
  }
}
